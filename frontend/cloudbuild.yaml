steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'NEXT_PUBLIC_GOOGLE_WEBRTC_URL=${_GOOGLE_WEBRTC_URL}'
      - '-t'
      - 'gcr.io/$PROJECT_ID/medlive-frontend'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/medlive-frontend'
timeout: '600s'
